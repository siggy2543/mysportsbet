import os

API_TOKEN = os.getenv("ESPN_API_TOKEN")
API_URL = os.getenv("ESPN_API_URL")

# Use environment variables to store sensitive data like API tokens.
# In Python, you can use the os module to access these variables.