"""
Test Configuration and Requirements
Defines test dependencies and pytest configuration
"""

# Test-specific requirements
test_requirements = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.1.0",
    "pytest-mock>=3.11.0",
    "pytest-xdist>=3.3.0",  # For parallel test execution
    "pytest-benchmark>=4.0.0",  # For performance benchmarking
    "pytest-timeout>=2.1.0",  # For test timeouts
    "httpx>=0.24.0",  # For async HTTP testing
    "aioresponses>=0.7.4",  # For mocking aiohttp responses
    "factory-boy>=3.3.0",  # For test data factories
    "faker>=19.0.0",  # For generating fake data
    "memory-profiler>=0.61.0",  # For memory profiling
    "psutil>=5.9.0",  # For system resource monitoring
    "coverage>=7.3.0",  # For code coverage
]

# Additional test databases
test_db_requirements = [
    "aiosqlite>=0.19.0",  # For SQLite async testing
    "sqlite3",  # Built-in SQLite support
]

print("Test requirements defined. Install with:")
print("pip install " + " ".join(test_requirements + test_db_requirements))